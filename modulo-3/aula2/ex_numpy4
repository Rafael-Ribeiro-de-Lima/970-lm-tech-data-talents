import numpy as np

'Transforme o array em uma matriz 3 por 4 utilizando numpy.'

arr = [1, 2, 3, 4, 'Amelia', 'Bruna', 'Carolina', 'Débora', True, False, False, True]

print(np.reshape(arr, (3, 4)))

